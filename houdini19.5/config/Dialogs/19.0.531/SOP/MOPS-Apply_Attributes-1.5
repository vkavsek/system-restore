// Magic: 8d33c470
// Automatically generated PSI dialog (PSI version 19.0.531)
// Mon Jul 11 11:44:44 2022


f_tmp = FLOAT_FIELD;	// dialog.top.ui

group.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    group.label = PARM_LABEL("Group", "group") WIDTH(1.5);
    PARM_FIELD(string, "group") VALUE(group.val) 
	LABELREF(group.label) HSTRETCH;

    group.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    ACTION_ICONBUTTON "BUTTONS_reselect" VALUE(group.script_action)
	HELP("Select geometry from an available viewport.")
	UI_FLAT_ICON_BTN_SIZE_SMALL LOOKFLAT;

    { WIDTH(0.1) }
}

grouptype.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Group Type", "grouptype") WIDTH(1.5);
    { WIDTH(0.09) }

    grouptype.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

sepparm.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

matchmode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Match By", "matchmode") WIDTH(1.5);
    { WIDTH(0.09) }

    matchmode.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

forcematch.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("forcematch", "Fallback to @ptnum") toggle VALUE(forcematch.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_translate.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_translate", "Do Translate") toggle VALUE(do_translate.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_rotate.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_rotate", "Do Rotate") toggle VALUE(do_rotate.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_scale.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_scale", "Do Scale") toggle VALUE(do_scale.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_pivots.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_pivots", "Do Pivots") toggle VALUE(do_pivots.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

sepparm2.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

local_space.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("local_space", "Local Space") toggle VALUE(local_space.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

rotate_mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Rotate Mode", "rotate_mode") WIDTH(1.5);
    { WIDTH(0.09) }

    rotate_mode.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

rotate_order.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Rotate Order", "rotate_order") WIDTH(1.5);
    { WIDTH(0.09) }

    rotate_order.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

scale_mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Scale Mode", "scale_mode") WIDTH(1.5);
    { WIDTH(0.09) }

    scale_mode.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_falloff.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_falloff", "Do Falloff") toggle VALUE(do_falloff.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

falloff_attribute.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    falloff_attribute.label = PARM_LABEL("Falloff Attribute", "falloff_attribute") WIDTH(1.5);
    PARM_FIELD(string, "falloff_attribute") VALUE(falloff_attribute.val) 
	LABELREF(falloff_attribute.label) HSTRETCH;

    { WIDTH(0.1) }
}

ignore_no_falloff.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("ignore_no_falloff", "Ignore Zero Falloff Prims") toggle VALUE(ignore_no_falloff.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_copy_attrs.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_copy_attrs", "Copy Attributes") toggle VALUE(do_copy_attrs.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

attribname.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    attribname.label = PARM_LABEL("Attributes To Copy", "attribname") WIDTH(1.5);
    PARM_FIELD(string, "attribname") VALUE(attribname.val) 
	LABELREF(attribname.label) HSTRETCH;

    attribname.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    { WIDTH(0.1) }
}

do_make_id.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_make_id", "Create Id Attr If Missing") toggle VALUE(do_make_id.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_orient.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_orient", "Output Orient Attribute") toggle VALUE(do_orient.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

do_output_scale.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_output_scale", "Output Scale Attribute") toggle VALUE(do_output_scale.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}
sepparm3.gad = NULL { }

folder0.gad = 
{
    LAYOUT(vertical) HSTRETCH

    TABDIALOG2 flat "folder0.gad"
    { 
	VALUE(folder0.gad.val) STRETCH(1,0)

	TAB "Transformation"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    do_translate.gad;		
	    do_rotate.gad;		
	    do_scale.gad;		
	    do_pivots.gad;		
	    sepparm2.gad;		
	    local_space.gad;		
	    rotate_mode.gad;		
	    rotate_order.gad;		
	    scale_mode.gad;		

	    { STRETCH(1,1) }
	}

	TAB "Falloff"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    do_falloff.gad;		
	    falloff_attribute.gad;		
	    ignore_no_falloff.gad;		

	    { STRETCH(1,1) }
	}

	TAB "Attributes"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    do_copy_attrs.gad;		
	    attribname.gad;		
	    do_make_id.gad;		
	    do_orient.gad;		
	    do_output_scale.gad;		

	    { STRETCH(1,1) }
	}
    } 
}

# warning cpp warning: "GenV3/subdialog.top.ui":5 Macro "PARMSPACING" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":6 Macro "LAYOUTDIR" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":7 Macro "DO_JUSTIFY" redefined.  Using later definition
dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    group.gad; 
    grouptype.gad; 
    sepparm.gad; 
    matchmode.gad; 
    forcematch.gad; 
    folder0.gad; 
    sepparm3.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
