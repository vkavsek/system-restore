// Magic: da40e8e8
// Automatically generated PSI dialog (PSI version 19.0.531)
// Mon Jul 11 11:34:36 2022


f_tmp = FLOAT_FIELD;	// dialog.top.ui

group.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    group.label = PARM_LABEL("Group", "group") WIDTH(1.5);
    PARM_FIELD(string, "group") VALUE(group.val) 
	LABELREF(group.label) HSTRETCH;

    group.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    ACTION_ICONBUTTON "BUTTONS_reselect" VALUE(group.script_action)
	HELP("Select geometry from an available viewport.")
	UI_FLAT_ICON_BTN_SIZE_SMALL LOOKFLAT;

    { WIDTH(0.1) }
}

grouptype.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Group Type", "grouptype") WIDTH(1.5);
    { WIDTH(0.09) }

    grouptype.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

falloff_attr.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    falloff_attr.label = PARM_LABEL("Falloff Attribute", "falloff_attr") WIDTH(1.5);
    PARM_FIELD(string, "falloff_attr") VALUE(falloff_attr.val) 
	LABELREF(falloff_attr.label) HSTRETCH;

    falloff_attr.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

    { WIDTH(0.1) }
}

blend_mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Blend Mode", "blend_mode") WIDTH(1.5);
    { WIDTH(0.09) }

    blend_mode.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

strength.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 strength.label = PARM_LABEL("Blend Strength", "strength") WIDTH(1.5);
 PARM(float, 1, "strength", none, none) RANGE(0,1)
	LOCK_RANGE LABELREF(strength.label) HSTRETCH;

  { WIDTH(0.1) }
}

switchclamp.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("switchclamp", "Clamp") toggle VALUE(switchclamp.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

sepparm.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

do_preview.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_preview", "Preview Falloff") toggle VALUE(do_preview.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}
spritemap.gad = NULL { }

falloff_type.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Falloff Type", "falloff_type") WIDTH(1.5);
    { WIDTH(0.09) }

    falloff_type.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

sphere_radius.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   sphere_radius.label = PARM_LABEL("Inner/Outer Radius", "sphere_radius") WIDTH(1.5);
   PARM(float, 2, "sphere_radius", none, none, "X,Y")
	RANGE(-1,1)  LABELREF(sphere_radius.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

box_scale.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   box_scale.label = PARM_LABEL("Scale", "box_scale") WIDTH(1.5);
   PARM(float, 3, "box_scale", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(box_scale.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

box_falloff.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 box_falloff.label = PARM_LABEL("Falloff", "box_falloff") WIDTH(1.5);
 PARM(float, 1, "box_falloff", none, none) RANGE(0,1)
	 LABELREF(box_falloff.label) HSTRETCH;

  { WIDTH(0.1) }
}

torus_size.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   torus_size.label = PARM_LABEL("Size", "torus_size") WIDTH(1.5);
   PARM(float, 2, "torus_size", none, none, "X,Y")
	RANGE(0,1)  LABELREF(torus_size.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

torus_falloff.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 torus_falloff.label = PARM_LABEL("Falloff", "torus_falloff") WIDTH(1.5);
 PARM(float, 1, "torus_falloff", none, none) RANGE(0,1)
	 LABELREF(torus_falloff.label) HSTRETCH;

  { WIDTH(0.1) }
}

cylinder_size.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   cylinder_size.label = PARM_LABEL("Size", "cylinder_size") WIDTH(1.5);
   PARM(float, 3, "cylinder_size", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(cylinder_size.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

cylinder_falloff.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 cylinder_falloff.label = PARM_LABEL("Falloff", "cylinder_falloff") WIDTH(1.5);
 PARM(float, 1, "cylinder_falloff", none, none) RANGE(0,1)
	 LABELREF(cylinder_falloff.label) HSTRETCH;

  { WIDTH(0.1) }
}

angle_range.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   angle_range.label = PARM_LABEL("Angle Range", "angle_range") WIDTH(1.5);
   PARM(float, 2, "angle_range", none, none, "X,Y")
	RANGE(0,1)  LABELREF(angle_range.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

falloff.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 falloff.label = PARM_LABEL("Falloff", "falloff") WIDTH(1.5);
 PARM(float, 1, "falloff", none, none) RANGE(-90,90)
	 LABELREF(falloff.label) HSTRETCH;

  { WIDTH(0.1) }
}

auto_fit.gad = ROW
{
    JUSTIFY(left, center)
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.1) }

    PARM_BUTTON("auto_fit", "Auto-Fit")
	push VALUE(auto_fit.val);

    { WIDTH(0.1) }
}

translate.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   translate.label = PARM_LABEL("Translate", "translate") WIDTH(1.5);
   PARM(float, 3, "translate", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(translate.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

rotate.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   rotate.label = PARM_LABEL("Rotate", "rotate") WIDTH(1.5);
   PARM(float, 3, "rotate", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(rotate.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

scale.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   scale.label = PARM_LABEL("Scale", "scale") WIDTH(1.5);
   PARM(float, 3, "scale", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(scale.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

uniform_scale.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 uniform_scale.label = PARM_LABEL("Uniform Scale", "uniform_scale") WIDTH(1.5);
 PARM(float, 1, "uniform_scale", none, none) RANGE(0,1)
	 LABELREF(uniform_scale.label) HSTRETCH;

  { WIDTH(0.1) }
}

do_noise.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_noise", "Add Noise") toggle VALUE(do_noise.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

input.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Noise Type", "input") WIDTH(1.5);
    { WIDTH(0.09) }

    input.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

use_rest.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("use_rest", "Use Rest Attribute") toggle VALUE(use_rest.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

amp.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   amp.label = PARM_LABEL("Amplitude", "amp") WIDTH(1.5);
   PARM(float, 3, "amp", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(amp.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

freq.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   freq.label = PARM_LABEL("Frequency", "freq") WIDTH(1.5);
   PARM(float, 4, "freq", none, none, "X,Y,Z,W")
	RANGE(0,1)  LABELREF(freq.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

uniform.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("uniform", "Uniform") toggle VALUE(uniform.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

roughness.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 roughness.label = PARM_LABEL("Roughness", "roughness") WIDTH(1.5);
 PARM(float, 1, "roughness", none, none) RANGE(0.0001,1)
	LOCK_RANGE_MIN LABELREF(roughness.label) HSTRETCH;

  { WIDTH(0.1) }
}

octaves.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 octaves.label = PARM_LABEL("Octaves", "octaves") WIDTH(1.5);
 PARM(integer, 1, "octaves", none, none) RANGE(1,10)
	LOCK_RANGE_MIN LABELREF(octaves.label) HSTRETCH;

  { WIDTH(0.1) }
}

offset.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   offset.label = PARM_LABEL("Offset", "offset") WIDTH(1.5);
   PARM(float, 4, "offset", none, none, "X,Y,Z,W")
	RANGE(0,1)  LABELREF(offset.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

tile_size.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   tile_size.label = PARM_LABEL("Tile Size", "tile_size") WIDTH(1.5);
   PARM(float, 3, "tile_size", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(tile_size.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

Distort.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   Distort.label = PARM_LABEL("Distort", "Distort") WIDTH(1.5);
   PARM(float, 3, "Distort", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(Distort.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

time_varying.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("time_varying", "Time Varying") toggle VALUE(time_varying.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

time_scale.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 time_scale.label = PARM_LABEL("Time Scale", "time_scale") WIDTH(1.5);
 PARM(float, 1, "time_scale", none, none) RANGE(0,1)
	 LABELREF(time_scale.label) HSTRETCH;

  { WIDTH(0.1) }
}

do_loop.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_loop", "Looping Noise") toggle VALUE(do_loop.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

period.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 period.label = PARM_LABEL("Loop Period", "period") WIDTH(1.5);
 PARM(float, 1, "period", none, none) RANGE(0,10)
	 LABELREF(period.label) HSTRETCH;

  { WIDTH(0.1) }
}

do_remap_3D.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_remap_3D", "Enable Remap") toggle VALUE(do_remap_3D.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

input_min_3D.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   input_min_3D.label = PARM_LABEL("Input Min", "input_min_3D") WIDTH(1.5);
   PARM(float, 3, "input_min_3D", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(input_min_3D.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

input_max_3D.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   input_max_3D.label = PARM_LABEL("Input Max", "input_max_3D") WIDTH(1.5);
   PARM(float, 3, "input_max_3D", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(input_max_3D.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

output_min_3D.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   output_min_3D.label = PARM_LABEL("Output Min", "output_min_3D") WIDTH(1.5);
   PARM(float, 3, "output_min_3D", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(output_min_3D.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

output_max_3D.gad = ROW
{
   HSTRETCH
   { WIDTH(0.18) }

   output_max_3D.label = PARM_LABEL("Output Max", "output_max_3D") WIDTH(1.5);
   PARM(float, 3, "output_max_3D", none, none, "X,Y,Z")
	RANGE(0,1)  LABELREF(output_max_3D.label)
	HSTRETCH;

    { WIDTH(0.1) }
}

remap_ramp_3D.si_rampedit = FLOAT_RAMP VALUE(remap_ramp_3D.ramp_val)
			LAYOUT(horizontal)  HSTRETCH;

remap_ramp_3D.multiramp = 
{
    LAYOUT(vertical) STRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

    ROW
    {
	JUSTIFY(left,center) SPACING(0.1) VMARGIN(0.02) HSTRETCH

	COL
	{
	    JUSTIFY(right,center) WIDTH(1.5)
	    LABEL "Point No.";
	}

	remap_ramp_3D.pointno.gad
	    = INT_SLIDER_FIELD VALUE(remap_ramp_3D.pointno.val) HSTRETCH;
    }
}

remap_ramp_3D.buttons.gad = COL
{
    VSTRETCH

    ICON_BUTTON_STRIP
    {
	LAYOUT(horizontal) INITSIZE(20p, 20p) 

	ACTION_ICONBUTTON "BUTTONS_delete"
		HELP("Remove Point") VALUE(remap_ramp_3D.delpoint.val);
	ACTION_ICONBUTTON "BUTTONS_add"
		HELP("Add Point") VALUE(remap_ramp_3D.addpoint.val);
    }

    { VSTRETCH }

    ROW
    {
	    remap_ramp_3D.toggleparms.gad
	      = ACTION_ICONBUTTON "BUTTONS_show_controls"
		HELP("Hide Controls", "h.pane.parms.toggle_ramp_parms")
		VALUE(remap_ramp_3D.toggle.val)
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;

	    remap_ramp_3D.togglerampsize.gad
	      = ACTION_ICONBUTTON "BUTTONS_restore_mini"
		HELP("Maximize ramp", "h.pane.parms.toggle_ramp_size")
		VALUE(remap_ramp_3D.togglerampsize.val)
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;
    }
}

remap_ramp_3D.parm =
{
    LAYOUT(vertical) HSTRETCH JUSTIFY(left, center)

    remap_ramp_3D.ramp.gad = ROW
    {
	MARGIN(0.02) SPACING(0.02) JUSTIFY(left, top) HSTRETCH

	remap_ramp_3D.buttons.gad VSTRETCH;

	remap_ramp_3D.si_rampedit HSTRETCH;
    }
}

remap_ramp_3D.gad =
{
    LAYOUT(vertical) MARGIN(2p) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

    {
	LAYOUT(horizontal) HSTRETCH

	PARM_LABEL_SIMPLE("Remap Ramp", "remap_ramp_3D");

	{ HSTRETCH }
	ACTION_ICONBUTTON "BUTTONS_ramp_flip_x" VALUE(remap_ramp_3D.reverse.val)
		LOOKFLAT HELP("Reverse Domain") UI_FLAT_ICON_BTN_SIZE_SMALL;
	ACTION_ICONBUTTON "BUTTONS_ramp_flip_y" VALUE(remap_ramp_3D.complement.val)
		LOOKFLAT HELP("Complement Ramp") UI_FLAT_ICON_BTN_SIZE_SMALL;
	remap_ramp_3D.presets
	    = ACTION_MENU_ICONBUTTON "BUTTONS_gear"
		LOOKFLAT HELP("Presets") UI_FLAT_ICON_BTN_SIZE_SMALL;

    }

    {
	LAYOUT(vertical) MARGIN(2p) SPACING(0.02) HSTRETCH

	remap_ramp_3D.parm;

	SWITCHER tight
	{
	    LAYOUT(overlap) VALUE(remap_ramp_3D.hideparms.val) HSTRETCH
	    remap_ramp_3D.multiramp;
	    {}
	}
    }
}

switchfit.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("switchfit", "Fit") toggle VALUE(switchfit.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

auto_min_max.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("auto_min_max", "Auto min and max input values") toggle VALUE(auto_min_max.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

frame.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 frame.label = PARM_LABEL("Reference Frame", "frame") WIDTH(1.5);
 PARM(float, 1, "frame", none, none) RANGE(0,10)
	 LABELREF(frame.label) HSTRETCH;

  { WIDTH(0.1) }
}

imin.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 imin.label = PARM_LABEL("Input Min", "imin") WIDTH(1.5);
 PARM(float, 1, "imin", none, none) RANGE(0,1)
	 LABELREF(imin.label) HSTRETCH;

  { WIDTH(0.1) }
}

imax.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 imax.label = PARM_LABEL("Input Max", "imax") WIDTH(1.5);
 PARM(float, 1, "imax", none, none) RANGE(0,1)
	 LABELREF(imax.label) HSTRETCH;

  { WIDTH(0.1) }
}

omin.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 omin.label = PARM_LABEL("Output Min", "omin") WIDTH(1.5);
 PARM(float, 1, "omin", none, none) RANGE(0,1)
	 LABELREF(omin.label) HSTRETCH;

  { WIDTH(0.1) }
}

omax.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 omax.label = PARM_LABEL("Output Max", "omax") WIDTH(1.5);
 PARM(float, 1, "omax", none, none) RANGE(0,1)
	 LABELREF(omax.label) HSTRETCH;

  { WIDTH(0.1) }
}

remap_falloff.si_rampedit = FLOAT_RAMP VALUE(remap_falloff.ramp_val)
			LAYOUT(horizontal)  HSTRETCH;

remap_falloff.multiramp = 
{
    LAYOUT(vertical) STRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

    ROW
    {
	JUSTIFY(left,center) SPACING(0.1) VMARGIN(0.02) HSTRETCH

	COL
	{
	    JUSTIFY(right,center) WIDTH(1.5)
	    LABEL "Point No.";
	}

	remap_falloff.pointno.gad
	    = INT_SLIDER_FIELD VALUE(remap_falloff.pointno.val) HSTRETCH;
    }
}

remap_falloff.buttons.gad = COL
{
    VSTRETCH

    ICON_BUTTON_STRIP
    {
	LAYOUT(horizontal) INITSIZE(20p, 20p) 

	ACTION_ICONBUTTON "BUTTONS_delete"
		HELP("Remove Point") VALUE(remap_falloff.delpoint.val);
	ACTION_ICONBUTTON "BUTTONS_add"
		HELP("Add Point") VALUE(remap_falloff.addpoint.val);
    }

    { VSTRETCH }

    ROW
    {
	    remap_falloff.toggleparms.gad
	      = ACTION_ICONBUTTON "BUTTONS_show_controls"
		HELP("Hide Controls", "h.pane.parms.toggle_ramp_parms")
		VALUE(remap_falloff.toggle.val)
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;

	    remap_falloff.togglerampsize.gad
	      = ACTION_ICONBUTTON "BUTTONS_restore_mini"
		HELP("Maximize ramp", "h.pane.parms.toggle_ramp_size")
		VALUE(remap_falloff.togglerampsize.val)
		LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;
    }
}

remap_falloff.parm =
{
    LAYOUT(vertical) HSTRETCH JUSTIFY(left, center)

    remap_falloff.ramp.gad = ROW
    {
	MARGIN(0.02) SPACING(0.02) JUSTIFY(left, top) HSTRETCH

	remap_falloff.buttons.gad VSTRETCH;

	remap_falloff.si_rampedit HSTRETCH;
    }
}

remap_falloff.gad =
{
    LAYOUT(vertical) MARGIN(2p) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

    {
	LAYOUT(horizontal) HSTRETCH

	PARM_LABEL_SIMPLE("Remap Falloff", "remap_falloff");

	{ HSTRETCH }
	ACTION_ICONBUTTON "BUTTONS_ramp_flip_x" VALUE(remap_falloff.reverse.val)
		LOOKFLAT HELP("Reverse Domain") UI_FLAT_ICON_BTN_SIZE_SMALL;
	ACTION_ICONBUTTON "BUTTONS_ramp_flip_y" VALUE(remap_falloff.complement.val)
		LOOKFLAT HELP("Complement Ramp") UI_FLAT_ICON_BTN_SIZE_SMALL;
	remap_falloff.presets
	    = ACTION_MENU_ICONBUTTON "BUTTONS_gear"
		LOOKFLAT HELP("Presets") UI_FLAT_ICON_BTN_SIZE_SMALL;

    }

    {
	LAYOUT(vertical) MARGIN(2p) SPACING(0.02) HSTRETCH

	remap_falloff.parm;

	SWITCHER tight
	{
	    LAYOUT(overlap) VALUE(remap_falloff.hideparms.val) HSTRETCH
	    remap_falloff.multiramp;
	    {}
	}
    }
}

do_sprite.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("do_sprite", "Display as Sprites") toggle VALUE(do_sprite.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

spritescale.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 spritescale.label = PARM_LABEL("Sprite Scale", "spritescale") WIDTH(1.5);
 PARM(float, 1, "spritescale", none, none) RANGE(0,10)
	 LABELREF(spritescale.label) HSTRETCH;

  { WIDTH(0.1) }
}

override_scale.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("override_scale", "Override Scale") toggle VALUE(override_scale.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

ramp_preset.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    PARM_LABEL_SIMPLE("Preset", "ramp_preset") WIDTH(1.5);
    { WIDTH(0.09) }

    ramp_preset.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}

falloff_heatmap.si_rampedit = COLOR_RAMP VALUE(falloff_heatmap.ramp_val)
			LAYOUT(horizontal)  HSTRETCH;

falloff_heatmap.multiramp = 
{
    LAYOUT(vertical) STRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

    ROW
    {
	JUSTIFY(left,center) SPACING(0.1) VMARGIN(0.02) HSTRETCH

	COL
	{
	    JUSTIFY(right,center) WIDTH(1.5)
	    LABEL "Point No.";
	}

	falloff_heatmap.pointno.gad
	    = INT_SLIDER_FIELD VALUE(falloff_heatmap.pointno.val) HSTRETCH;
    }
}

falloff_heatmap.buttons.gad = COL
{
    VSTRETCH

    ICON_BUTTON_STRIP
    {
	LAYOUT(horizontal) INITSIZE(20p, 20p) 

	ACTION_ICONBUTTON "BUTTONS_delete"
		HELP("Remove Point") VALUE(falloff_heatmap.delpoint.val);
	ACTION_ICONBUTTON "BUTTONS_add"
		HELP("Add Point") VALUE(falloff_heatmap.addpoint.val);
    }

    { VSTRETCH }

    falloff_heatmap.toggleparms.gad
      = ACTION_ICONBUTTON "BUTTONS_show_controls"
	HELP("Hide Controls", "h.pane.parms.toggle_ramp_parms")
	VALUE(falloff_heatmap.toggle.val)
	LOOKFLAT UI_FLAT_ICON_BTN_SIZE_TINY;
}

falloff_heatmap.parm =
{
    LAYOUT(vertical) HSTRETCH JUSTIFY(left, center)

    falloff_heatmap.ramp.gad = ROW
    {
	MARGIN(0.02) SPACING(0.02) JUSTIFY(left, top) HSTRETCH

	falloff_heatmap.buttons.gad VSTRETCH;

	falloff_heatmap.si_rampedit HSTRETCH;
    }
}

falloff_heatmap.gad =
{
    LAYOUT(vertical) MARGIN(2p) HSTRETCH
    HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE
    {
	LAYOUT(horizontal) HSTRETCH

	PARM_LABEL_SIMPLE("Falloff Ramp", "falloff_heatmap");

	{ HSTRETCH }
	ACTION_ICONBUTTON "BUTTONS_ramp_flip_x" VALUE(falloff_heatmap.reverse.val)
		LOOKFLAT HELP("Reverse Domain") UI_FLAT_ICON_BTN_SIZE_SMALL;
	ACTION_ICONBUTTON "BUTTONS_ramp_flip_y" VALUE(falloff_heatmap.complement.val)
		LOOKFLAT HELP("Complement Ramp") UI_FLAT_ICON_BTN_SIZE_SMALL;
	falloff_heatmap.presets
	    = ACTION_MENU_ICONBUTTON "BUTTONS_gear"
		LOOKFLAT HELP("Presets") UI_FLAT_ICON_BTN_SIZE_SMALL;

    }

    {
	LAYOUT(vertical) MARGIN(2p) SPACING(0.02) HSTRETCH

	falloff_heatmap.parm;

	SWITCHER tight
	{
	    LAYOUT(overlap) VALUE(falloff_heatmap.hideparms.val) HSTRETCH
	    falloff_heatmap.multiramp;
	    {}
	}
    }
}

fitparams.gad = 

GROUP simple "Fit" flat 
{ 
    VALUE(fitparams.gad.val) LAYOUT(vertical) HSTRETCH 
	    switchfit.gad;		
	    auto_min_max.gad;		
	    frame.gad;		
	    imin.gad;		
	    imax.gad;		
	    omin.gad;		
	    omax.gad;		
} 

folder7.gad = 
{
    LAYOUT(vertical) HSTRETCH

    TABDIALOG2 flat "folder7.gad"
    { 
	VALUE(folder7.gad.val) STRETCH(1,0)

	TAB "Noise Controls"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    input.gad;		
	    use_rest.gad;		
	    amp.gad;		
	    freq.gad;		
	    uniform.gad;		
	    roughness.gad;		
	    octaves.gad;		
	    offset.gad;		
	    tile_size.gad;		
	    Distort.gad;		
	    time_varying.gad;		
	    time_scale.gad;		
	    do_loop.gad;		
	    period.gad;		

	    { STRETCH(1,1) }
	}

	TAB "Noise Remap"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    do_remap_3D.gad;		
	    input_min_3D.gad;		
	    input_max_3D.gad;		
	    output_min_3D.gad;		
	    output_max_3D.gad;		
	    remap_ramp_3D.gad;		

	    { STRETCH(1,1) }
	}
    } 
}

folder0.gad = 

GROUP simple "Transform" flat 
{ 
    VALUE(folder0.gad.val) LAYOUT(vertical) HSTRETCH 
	    auto_fit.gad;		
	    translate.gad;		
	    rotate.gad;		
	    scale.gad;		
	    uniform_scale.gad;		
} 

folder1.gad = 

GROUP simple "Radial Wipe" flat 
{ 
    VALUE(folder1.gad.val) LAYOUT(vertical) HSTRETCH 
	    angle_range.gad;		
	    falloff.gad;		
} 

folder5.gad = 

GROUP simple "Cylinder" flat 
{ 
    VALUE(folder5.gad.val) LAYOUT(vertical) HSTRETCH 
	    cylinder_size.gad;		
	    cylinder_falloff.gad;		
} 

folder4.gad = 

GROUP simple "Torus" flat 
{ 
    VALUE(folder4.gad.val) LAYOUT(vertical) HSTRETCH 
	    torus_size.gad;		
	    torus_falloff.gad;		
} 

folder3.gad = 

GROUP simple "Box" flat 
{ 
    VALUE(folder3.gad.val) LAYOUT(vertical) HSTRETCH 
	    box_scale.gad;		
	    box_falloff.gad;		
} 

folder2.gad = 

GROUP simple "Sphere" flat 
{ 
    VALUE(folder2.gad.val) LAYOUT(vertical) HSTRETCH 
	    sphere_radius.gad;		
} 

folder6.gad = 
{
    LAYOUT(vertical) HSTRETCH

    TABDIALOG2 flat "folder6.gad"
    { 
	VALUE(folder6.gad.val) STRETCH(1,0)

	TAB "Shape"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    falloff_type.gad;		
	    folder2.gad;		
	    folder3.gad;		
	    folder4.gad;		
	    folder5.gad;		
	    folder1.gad;		
	    folder0.gad;		

	    { STRETCH(1,1) }
	}

	TAB "Noise"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    do_noise.gad;		
	    folder7.gad;		

	    { STRETCH(1,1) }
	}

	TAB "Remap"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    fitparams.gad;		
	    remap_falloff.gad;		

	    { STRETCH(1,1) }
	}

	TAB "Falloff Preview"
	{
	    LAYOUT(vertical) STRETCH

            HMARGIN(0) VMARGIN(2p) SPACING(0, 2p)
	    do_sprite.gad;		
	    spritescale.gad;		
	    override_scale.gad;		
	    ramp_preset.gad;		
	    falloff_heatmap.gad;		

	    { STRETCH(1,1) }
	}
    } 
}

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    group.gad; 
    grouptype.gad; 
    falloff_attr.gad; 
    blend_mode.gad; 
    strength.gad; 
    switchclamp.gad; 
    sepparm.gad; 
    do_preview.gad; 
    spritemap.gad; 
    folder6.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
